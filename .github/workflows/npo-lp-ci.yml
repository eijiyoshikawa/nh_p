name: npo-lp CI

on:
  pull_request:
    paths:
      - "npo-lp/**"
      - ".github/workflows/npo-lp-ci.yml"
  push:
    branches: [main]
    paths:
      - "npo-lp/**"
      - ".github/workflows/npo-lp-ci.yml"

concurrency:
  group: npo-lp-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: lint + build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: npo-lp
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: npo-lp/.nvmrc
          cache: npm
          cache-dependency-path: npo-lp/package-lock.json

      - run: npm ci

      - run: npm run lint

      - run: npm run build
